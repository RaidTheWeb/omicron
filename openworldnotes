Streaming in large maps:

Load every chunk's (in renderable distance) far away LODs and terrain (etc.) and then for closer chunks load in closer LODs.

- Terrain can be loaded for every chunk.
- Low resolution far away textures.
- Low quality LODs for trees, buildings, etc.

- Load higher quality LODs
- Load higher resolution textures

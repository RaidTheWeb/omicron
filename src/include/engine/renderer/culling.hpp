#ifndef _ENGINE__RENDERER__CULLING_HPP
#define _ENGINE__RENDERER__CULLING_HPP

#include <engine/math/math.hpp>
#include <engine/utils/memory.hpp>

// https://www.beosil.com/download/CollisionDetectionHashing_VMV03.pdf
// Proposes a grid cell system for collision detection, however, we can use this grid cell system for culling and apply it to almost anything.

namespace OCull {
    static inline uint32_t hashindices(void) {
        return (uint32_t)
    }
};


#endif
